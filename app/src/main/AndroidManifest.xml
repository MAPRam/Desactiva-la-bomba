<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ox5.bomba">

    <uses-permission android:name="android.permission.WRITE_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:screenOrientation="landscape"
       <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectBomba" />
        <activity android:name=".Ajustes" />
        <activity android:name=".Puntaje" />
        <activity android:name=".perdio" />
        <activity android:name=".creditos" />
        <activity android:name=".gana"/>



    </application>

</manifest>